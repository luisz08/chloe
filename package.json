{
  "name": "chloe",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test",
    "check": "bunx biome check --error-on-warnings .",
    "typecheck": "bunx tsc --noEmit -p tsconfig.check.json"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "@biomejs/biome": "^1.9.0",
    "@types/turndown": "^5.0.6",
    "bun-types": "^1.1.0"
  },
  "dependencies": {
    "node-html-parser": "^7.1.0",
    "turndown": "^7.2.4"
  }
}
